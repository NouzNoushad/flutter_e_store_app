import 'package:flutter/material.dart';

const primarySwatch = Colors.blue;

const primaryColor = Color.fromRGBO(39, 47, 63, 1);
const lightPrimaryColor = Color.fromRGBO(28, 34, 46, 1);
const whiteColor = Colors.white;
const amberColor = Colors.amber;
